version: 2
jobs:
  build:
    docker:
      - image: google/dart
    steps:
      - checkout
      - run: |
          cd mobx
          pub get
          dartanalyzer --fatal-infos --fatal-warnings .
          dart --enable_asserts --enable-vm-service test/all.dart