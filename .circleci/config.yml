version: 2
jobs:
  build:
    docker:
      - image: google/dart
    steps:
      - checkout
      - run: ls
      - run: cd mobx
      - run: ls
      - run: pub get
      - run: dartanalyzer --fatal-infos --fatal-warnings .