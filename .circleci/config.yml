version: 2
jobs:
  build:
    docker:
      - image: google/dart
    steps:
      - checkout
      - run: echo $PWD
      - run: cd mobx
      - run: echo $PWD
      - run: pub get
      - run: dartanalyzer --fatal-infos --fatal-warnings .