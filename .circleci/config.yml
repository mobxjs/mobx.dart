version: 2
jobs:
  build_mobx:
    docker:
      - image: google/dart
    environment:
      - PACKAGE: mobx
    steps:
      - checkout
      - run: tool/circle_run.sh

  build_mobx_codegen:
    docker:
      - image: google/dart
    environment:
      - PACKAGE: mobx_codegen
    steps:
      - checkout
      - run: tool/circle_run.sh

  build_mobx_codegen_example:
    docker:
      - image: google/dart
    environment:
      - PACKAGE: mobx_codegen/example
    steps:
      - checkout
      - run: tool/circle_run.sh

workflows:
  version: 2
  mobx_core:
    jobs:
      - build_mobx
      - build_mobx_codegen
      - build_mobx_codegen_example