name: mobx_example
description: Examples for MobX

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  mobx: ^2.6.1

dependency_overrides:
  mobx:
    path: ../
  mobx_codegen:
    path: ../../mobx_codegen

dev_dependencies:
  build_runner: ^2.16.1
  lints: ^6.1.0
  mobx_codegen: ^2.7.8

publish_to: none
